
 {8}  Build pipeline project in Jenkins.
                           
⚙️ 1: Install the Pipeline Plugin
Go to Manage Jenkins → Plugins → Available Plugins
Search for “Pipeline”
Select it → click Install without restart
Restart Jenkins if required

🧩 2: Create a New Pipeline Project
From Jenkins Dashboard → click “New Item”
Enter a name (e.g., MyPipeline)
Select “Pipeline” → click OK

🧱 3: Configure the Pipeline
Scroll down to the Pipeline section
You have two options:
Option 1: Pipeline Script (direct input)
Choose “Pipeline script”
Add sample script:

pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                sh 'mvn clean install'
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                sh 'mvn test'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying application...'
            }
        }
    }
}


Option 2: Pipeline Script from SCM (GitHub, etc.)
Choose “Pipeline script from SCM”
Select Git and enter your repo URL
Set Script Path to your Jenkinsfile

⚡4: Save and Build
Click Save
Click Build Now 
Check build stages in the Stage View (it shows each stage visually)

✅ 5: Verify
If each stage (Build, Test, Deploy) turns green, pipeline is successful 🎉
You can click each stage to view detailed logs


 {9} Perform various Git operations on local and remote repositories.
                                       
            git config --global user.name "Musab Shaikh"
git config --global user.email "musabshaikh@example.com"
mkdir GitPractical
cd GitPractical
git init
echo "This is file1" > file1.txt
echo "This is file2" > file2.txt
git status
git add file1.txt
git add .
git commit -m "Add initial files file1.txt and file2.txt"
git log
git branch                   # List branches
git branch feature1           # Create branch 'feature1'
git checkout feature1         # Switch to 'feature1'
echo "Feature work" > feature.txt
git add feature.txt
git commit -m "Add feature.txt in feature1 branch"
git checkout main             # Switch back to main
git merge feature1            # Merge feature1 into main
git branch -d feature1        # Delete 'feature1'
git remote add origin https://github.com/username/GitPractical.git
git remote -v
git push -u origin main  # Push local commits to remote main
git pull                 # Fetch and merge latest changes
cd ..
git clone https://github.com/username/GitPractical.git
cd GitPractical
echo "Change in file1" >> file1.txt
git diff                     # Shows changes in file1.txt
git restore file1.txt         # Undo changes in file1.txt
git rm file2.txt              # Remove file2.txt from repo
git commit -m "Remove file2.txt"
git fetch                     # Fetch latest commits from remote
git reset --hard              # Reset local repo to last commit

10
# Step 1: Pull Ubuntu image
docker pull ubuntu

# Step 2: Verify image is downloaded
docker images

# Step 3: Run Ubuntu container with bash
docker run -it ubuntu bash

# Step 4: Inside container, run Linux commands
ls
pwd
echo "Hello Docker"

# Step 5: Exit container
exit

# Step 6: List containers
docker ps -a

11
# Clone repository from GitHub
git clone https://github.com/username/my-repository.git
cd my-repository

# Check current branch (optional, but useful)
git branch

# Pull latest changes from remote
git pull
# Alternative if branch has no upstream set:
# git branch --set-upstream-to=origin/main main
# git pull

# Make changes to files
echo "This is a new line" >> file1.txt

# Stage changes
git add file1.txt          # Option 1: add a specific file
git add .                  # Option 2: add all changes

# Commit changes
git commit -m "Add new line to file1.txt"

# Push changes to GitHub
git push                   # Option 1: push normally
git push -u origin main    # Option 2: first push with upstream set



12
# Step 1: Pull an image (example: Ubuntu, Alpine)
docker pull ubuntu
docker pull alpine

# Step 2: List all images on local machine
docker images

# Step 3: Remove an image you no longer need (example: Alpine)
docker rmi alpine

# Step 4: Verify image removal
docker images

13
# Step 1: Run a container interactively (example: Ubuntu)
docker run -it ubuntu bash

# Step 2: Inside container, execute basic Linux commands
ls
pwd
mkdir test
echo "This is a file" > test/file1.txt
cat test/file1.txt
rm test/file1.txt
exit

# Step 3: Back on host, list containers
docker ps -a

14 
git config --global user.name "Musab Shaikh"
git config --global user.email "musabshaikh@example.com"
mkdir GitBasic
cd GitBasic
git init
git status
echo "Hello Git" > file1.txt
git add file1.txt      # Stage a single file
git add .              # Stage all files (alternative)
git commit -m "Add file1.txt"
git log
git branch                  # List branches
git branch feature1          # Create a branch
git checkout feature1        # Switch to branch
git checkout -b feature2     # Create and switch to new branch
git merge feature1           # Merge branch into current
git branch -d feature1       # Delete branch
git restore file1.txt        # Undo unstaged changes
git reset --hard             # Reset repository to last commit (discard all changes)
git rm file1.txt             # Remove file from repo
git remote add origin <url>  # Connect local repo to GitHub
git remote -v                # View connected remotes
git push -u origin main      # Push commits first time
git push                     # Push subsequent commits
git pull                     # Fetch and merge latest changes
git clone <url>              # Copy remote repo locally

15

mkdir GitRepo
cd GitRepo
git init
echo "This is my first Git file" > file1.txt
git status
git add file1.txt
# Or stage all files
git add .
git commit -m "Add initial file file1.txt to repository"
git remote add origin https://github.com/username/my-repository.git
git remote -v   # Verify remote
git push -u origin main   # First push with upstream
# Or if upstream is already set
git push

17
# Step 1: Create a Dockerfile (example)
nano Dockerfile
# Dockerfile contents:
# FROM ubuntu
# RUN apt update && apt install -y cowsay
# CMD ["cowsay", "Hello from custom image"]

# Step 2: Build Docker image
docker build -t myubuntu .

# Step 3: Verify image is created
docker images

# Step 4: Run container from custom image
docker run -it myubuntu

# Step 5: Exit container
exit

